backend:
  name: github
  repo: zuqiu2024/zxx
  branch: master  # ⚠️ 请务必确认是 `master` 还是 `main`，必须与仓库默认分支一致！
  use_git_gateway: false
  base_url: https://zhanxx1.de5.net  # 🚨 **必须添加！** 指向你的管理员站点

media_folder: "public/images"
public_folder: "/images"

collections:
  - name: "posts"
    label: "文章"
    folder: "src/content/posts"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "标题", name: "title", widget: "string" }
      - { label: "发布日期", name: "published", widget: "datetime", format: "YYYY-MM-DD" }
      - { label: "描述", name: "description", widget: "text" }
      - { label: "标签", name: "tags", widget: "list" }
      - { label: "封面图片", name: "image", widget: "string" }
      - { label: "作者", name: "author", widget: "string", default: "展" }
      - { label: "正文", name: “body", widget: "markdown" }